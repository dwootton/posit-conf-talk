/* Same twind setup the vibe-widget AppWrapper gives generated code (tw / css). */
import { setup, tw, css } from "@twind/core";
import config from "__TWIND_CONFIG__";
setup(config);
export { tw, css };
