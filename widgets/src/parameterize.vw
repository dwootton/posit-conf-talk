{
  "version": "1.0",
  "created_at": "2026-09-16T11:12:45",
  "description": "Houston donut shops.\n\nDATA: the `data` input is a list of shops, one row per shop, with columns: shop_id, name, neighborhood, latitude, longitude, rating (1-5), review_count, price_level (1-3), weekday_wait_min, weekend_wait_min, pumpkin_spice (boolean), open_late (boolean), signature_donut.\nBASEMAP: the `basemap` input is a GeoJSON FeatureCollection giving Houston context, bbox [-95.615, 29.600, -95.255, 29.880]. Each feature has properties.kind: \"bayou\" (LineString), \"loop\" (LineString ring for the Interstate 610 loop), or \"neighborhood\" (Point, with properties.name).\n\nMAP RULES: render the map as one inline SVG with d3 (import * as d3 from \"https://esm.sh/d3@7\"). Use d3.geoMercator with fitExtent on the basemap bbox so the whole bbox is visible inside the map area. Draw in this order: bayous as thin muted lines, the loop as a thin dashed ring, neighborhood names as small uppercase monospace labels in a low-emphasis color, then the shop marks on top. No tile layers, no external map libraries, no network requests at runtime other than the d3 import. Distances are great-circle miles from the haversine formula. Position the tooltip inside the map bounds.\n\nLAYOUT RULES: the widget is exactly 814x548 pixels, background #f2f0e9, no scrollbars, no responsiveness to window size. Fonts: 'Space Grotesk', sans-serif for text and 'JetBrains Mono', monospace for numbers, labels and small caps; both are provided by the host, do not load fonts.\n\nCOPY RULES (strict): no title or heading, no explanatory paragraphs, no instructions longer than one short line, no code or SQL readouts, no placeholder text, no emoji, no decorative badges, no record counts unless they are the result of the interaction. Every number shown is computed from the data.\n\nQuery parameterization: the interaction sets the parameters of a fixed query (a center and a radius).\nTwo-column layout: the map fills the left 58% of the width at full height; a control panel fills the right side; a 2px #1a1a1a vertical rule separates them.\nThe search region is a circle around a target. The target is a crosshair marker; dragging anywhere on the map moves it (pointer drag with live updates) and arrow keys nudge it. The radius comes from a slider in the panel (0.25 to 4 miles, step 0.05, default 0.8) whose current value is shown beside it as e.g. \"0.80 mi\". Draw the region as a circle filled #3f8bdb at low opacity with a dashed #3f8bdb outline. Shops inside the region are filled #3f8bdb with a white outline; shops outside are small muted hollow circles.\nPanel, top to bottom: the radius slider row; four stat tiles in a 2x2 grid computed from the shops inside the region: shops in range (count), mean rating (two decimals), mean weekend wait (rounded minutes), most common price level shown as $ signs; then a ranked list of up to five shops inside the region sorted by distance, each with name, neighborhood, and distance in miles (two decimals). If no shop is inside, the list is one short line: \"No shops in range\". Hover on a shop shows a card tooltip with name, neighborhood, distance to the target.\n\nREVISION: Bug fix only. The map collapses every mark to a single point because the projection is fitted with fitExtent on a Polygon built from the bbox; d3-geo reads that ring's winding as the whole sphere. Replace that polygon with a MultiPoint of the two bbox corners: fitExtent(extent, { type: \"MultiPoint\", coordinates: [[-95.615, 29.600], [-95.255, 29.880]] }). Keep every other line, style, label and interaction exactly as it is.\n\nREVISION: Default state fix only. The target starts at [-95.435, 29.740], far from every shop, so the widget opens with zero shops in range. Start the target at longitude -95.3862, latitude 29.7452 instead (between Montrose and Midtown) so several shops are inside the default 0.80 mile region on load. Change nothing else.\n\nREVISION: Two changes to make everywhere in this file, on top of whatever else this request asks for:\n\n1) REAL BASEMAP. A new input `basemap_image` is provided: a data: URL (JPEG) of a real, already-styled Houston street map -- roads, water, and neighborhood names are baked into the image itself. Its corners are NW corner [lon -95.615, lat 29.880], SE corner [lon -95.255, lat 29.600]. Read it with model.get(\"basemap_image\"). Render it FIRST, as a single SVG <image href={basemapImage}>: project the two bbox corners above with the SAME d3 projection used for the shop marks to get its on-screen rectangle (x, y, width, height from the two projected points), and set preserveAspectRatio=\"none\" so it exactly fills that rectangle -- this is what keeps the image aligned with the marks. Then DELETE every bit of code that draws your own roads, water, bayous, the 610 loop, neighborhood-name text labels, a background grid, or any bounding/frame circle around the whole map -- the image already shows all of that, and drawing your own on top would duplicate and clash with it. If the `basemap` GeoJSON input still exists in the code, remove every use of it along with any now-dead helper code, styles, or legend entries that only existed to describe those removed line-drawn elements. Keep every shop mark, overlay (target, search region, brush, connecting lines), tooltip and panel exactly as they are, just drawn on top of the new image instead of the old lines.\n\n2) POINTER MATH SAFETY. This widget renders inside an iframe inside a scaled presentation slide, which breaks two common shortcuts. Find every place in this file that converts a mouse/pointer position into a map coordinate and fix it if needed: NEVER use d3.drag's `event.x`/`event.y`, and NEVER use `d3.pointer(event, node)` -- both rely on SVGElement.getScreenCTM(), which does not account for an ancestor iframe living inside a CSS-transformed (scaled) page and silently returns the wrong offset there, producing a drag that visibly lags/jumps away from the cursor. Instead, always compute the position by hand: read the svg element's own `getBoundingClientRect()` and subtract its `left`/`top` from the event's `clientX`/`clientY` (inside a d3.drag `.on(\"drag\", ...)` handler, read those off `event.sourceEvent.clientX`/`event.sourceEvent.clientY`, not off `event.x`/`event.y`). If there is a rectangular brush/marquee-select interaction implemented with d3.brush(), replace it with a hand-rolled version using plain pointerdown/pointermove/pointerup listeners and an SVG <rect> you draw yourself, computing every corner the same getBoundingClientRect way -- d3.brush() has the identical getScreenCTM problem internally. Keep the resulting interaction pixel-identical to how it behaves today; this is a coordinate-math fix only, not a redesign.\n\nREVISION: Legibility only -- this widget is projected on a large screen from the back of a conference room, so nothing about its behaviour, layout or colours should change.\n\nRaise every piece of text in the widget to at least 15px. Any number that is the result of the interaction -- a count, a mean, a headline figure -- goes to at least 28px. Tooltip text 15px. Axis tick labels 14px and axis titles 15px, with at most four ticks on each axis. Legend and button labels 15px.\n\nWhere a label is only decoration and is not needed to read the result, remove it rather than shrink anything else to make room. Do not add any new text.\n\nChange nothing else: same marks, same colours, same interactions, same panel structure, same computed values.\n\nREVISION: Layout fix only. After the last change the widget's content is 555px tall, but the widget is exactly 814x548 and anything past that is clipped, so the bottom of the right-hand panel is being cut off.\n\nMake the whole widget fit inside 548px of height again, with nothing clipped and no scrollbar. Do it by tightening vertical spacing in the right-hand panel -- reduce the gaps between the stat tiles, the ranked list rows and the slider row, and trim the panel's outer padding -- and if that is still not enough, show four ranked shops instead of five.\n\nDo NOT reduce any font size to make room: every piece of text must stay at least 15px, and the stat-tile numbers must stay at least 28px. Change nothing else: same map, same marks, same drag and slider behaviour, same computed values.\n\nREVISION: Layout fix only, and the previous attempt did not work -- the content is still 555px tall inside a widget that is exactly 814 x 548, so the bottom 7px is clipped off.\n\nFind the outermost container of the right-hand panel and make the whole right column a flex column with a FIXED height of 548px, boxSizing 'border-box', overflow 'hidden', and its vertical padding reduced to 10px top and bottom. Then remove roughly 10px of vertical space from inside it: cut the gaps between the slider row, the 2x2 stat tile grid and the ranked list, and cut the padding inside each ranked-list row. If after that the content still does not fit, render four ranked shops instead of five.\n\nDo not change any font size: all text stays at 15px or larger and the stat-tile numbers stay at 28px or larger. Do not change the map, the marks, the drag behaviour, the slider, or any computed value.",
  "code": "import * as d3 from \"https://esm.sh/d3@7\";\n\nexport const haversine = (lat1, lon1, lat2, lon2) => {\n  const R = 3958.8;\n  const dLat = (lat2 - lat1) * Math.PI / 180;\n  const dLon = (lon2 - lon1) * Math.PI / 180;\n  const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +\n            Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *\n            Math.sin(dLon / 2) * Math.sin(dLon / 2);\n  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));\n  return R * c;\n};\n\nexport const getMode = (arr) => {\n  if (!arr || arr.length === 0) return null;\n  const counts = {};\n  let maxCount = 0;\n  let mode = arr[0];\n  for (const val of arr) {\n    counts[val] = (counts[val] || 0) + 1;\n    if (counts[val] > maxCount) {\n      maxCount = counts[val];\n      mode = val;\n    }\n  }\n  return mode;\n};\n\nexport const StatTile = ({ React, label, value }) => (\n  <div style={{\n    background: \"#f7f0e6\",\n    border: \"2px solid #1a1a1a\",\n    padding: \"8px 12px\",\n    boxShadow: \"2px 2px 0 #1a1a1a\",\n    display: \"flex\",\n    flexDirection: \"column\",\n    gap: \"2px\"\n  }}>\n    <div style={{\n      fontFamily: \"'JetBrains Mono', monospace\",\n      fontSize: \"15px\",\n      textTransform: \"uppercase\",\n      color: \"#4b5563\",\n      letterSpacing: \"0.5px\"\n    }}>\n      {label}\n    </div>\n    <div style={{\n      fontFamily: \"'JetBrains Mono', monospace\",\n      fontSize: \"28px\",\n      fontWeight: 600,\n      color: \"#1a1a1a\"\n    }}>\n      {value}\n    </div>\n  </div>\n);\n\nexport const SliderRow = ({ React, value, onChange }) => (\n  <div style={{ display: \"flex\", alignItems: \"center\", gap: \"12px\", paddingBottom: \"8px\", borderBottom: \"2px solid #1a1a1a\" }}>\n    <input\n      type=\"range\"\n      min=\"0.25\"\n      max=\"4\"\n      step=\"0.05\"\n      value={value}\n      onChange={e => onChange(parseFloat(e.target.value))}\n      style={{ flex: 1, accentColor: \"#f97316\", cursor: \"pointer\" }}\n    />\n    <div style={{\n      fontFamily: \"'JetBrains Mono', monospace\",\n      fontSize: \"28px\",\n      fontWeight: 600,\n      color: \"#1a1a1a\",\n      width: \"140px\",\n      textAlign: \"right\"\n    }}>\n      {value.toFixed(2)} mi\n    </div>\n  </div>\n);\n\nconst WIDTH = 472;\nconst HEIGHT = 548;\nconst NW = [-95.615, 29.880];\nconst SE = [-95.255, 29.600];\n\nexport default function Widget({ model, React }) {\n  const [data, setData] = React.useState(model.get(\"data\") || []);\n  const [basemapImage, setBasemapImage] = React.useState(model.get(\"basemap_image\") || \"\");\n\n  React.useEffect(() => {\n    const handleData = () => setData(model.get(\"data\") || []);\n    const handleBasemapImage = () => setBasemapImage(model.get(\"basemap_image\") || \"\");\n\n    model.on(\"change:data\", handleData);\n    model.on(\"change:basemap_image\", handleBasemapImage);\n\n    return () => {\n      model.off(\"change:data\", handleData);\n      model.off(\"change:basemap_image\", handleBasemapImage);\n    };\n  }, [model]);\n\n  const [target, setTarget] = React.useState([-95.3862, 29.7452]);\n  const [radius, setRadius] = React.useState(0.8);\n  const [hovered, setHovered] = React.useState(null);\n\n  const mapRef = React.useRef(null);\n  const sceneRef = React.useRef(null);\n  const setTargetRef = React.useRef(setTarget);\n  setTargetRef.current = setTarget;\n\n  React.useEffect(() => {\n    model.set(\"target_lon\", target[0]);\n    model.set(\"target_lat\", target[1]);\n    model.set(\"search_radius\", radius);\n    model.save_changes();\n  }, [target, radius, model]);\n\n  const shopsWithDist = React.useMemo(() => {\n    return data.map(s => ({\n      ...s,\n      distance: haversine(target[1], target[0], s.latitude, s.longitude)\n    }));\n  }, [data, target]);\n\n  const inRange = React.useMemo(() => {\n    return shopsWithDist\n      .filter(s => s.distance <= radius)\n      .sort((a, b) => a.distance - b.distance);\n  }, [shopsWithDist, radius]);\n\n  const handleKeyDown = (e) => {\n    const step = 0.005;\n    if (e.key === 'ArrowUp') { e.preventDefault(); setTarget(t => [t[0], t[1] + step]); }\n    if (e.key === 'ArrowDown') { e.preventDefault(); setTarget(t => [t[0], t[1] - step]); }\n    if (e.key === 'ArrowLeft') { e.preventDefault(); setTarget(t => [t[0] - step, t[1]]); }\n    if (e.key === 'ArrowRight') { e.preventDefault(); setTarget(t => [t[0] + step, t[1]]); }\n  };\n\n  React.useEffect(() => {\n    if (!mapRef.current) return;\n\n    const container = d3.select(mapRef.current);\n    container.selectAll(\"*\").remove();\n\n    const svg = container.append(\"svg\")\n      .attr(\"width\", WIDTH)\n      .attr(\"height\", HEIGHT)\n      .style(\"background\", \"#f7f0e6\");\n\n    const projection = d3.geoMercator().fitExtent(\n      [[16, 16], [WIDTH - 16, HEIGHT - 16]],\n      { type: \"MultiPoint\", coordinates: [NW, SE] }\n    );\n    const path = d3.geoPath().projection(projection);\n\n    const drag = d3.drag().on(\"drag\", (event) => {\n      const svgNode = svg.node();\n      const rect = svgNode.getBoundingClientRect();\n      const srcEvent = event.sourceEvent;\n\n      let clientX = srcEvent.clientX;\n      let clientY = srcEvent.clientY;\n      if (srcEvent.touches && srcEvent.touches.length > 0) {\n        clientX = srcEvent.touches[0].clientX;\n        clientY = srcEvent.touches[0].clientY;\n      }\n\n      const x = clientX - rect.left;\n      const y = clientY - rect.top;\n\n      const [lon, lat] = projection.invert([x, y]);\n      const cLon = Math.max(NW[0], Math.min(SE[0], lon));\n      const cLat = Math.max(SE[1], Math.min(NW[1], lat));\n      setTargetRef.current([cLon, cLat]);\n    });\n\n    svg.append(\"rect\")\n      .attr(\"width\", WIDTH)\n      .attr(\"height\", HEIGHT)\n      .attr(\"fill\", \"transparent\")\n      .style(\"cursor\", \"crosshair\")\n      .call(drag);\n\n    if (basemapImage) {\n      const nw = projection(NW);\n      const se = projection(SE);\n\n      svg.append(\"image\")\n        .attr(\"href\", basemapImage)\n        .attr(\"x\", nw[0])\n        .attr(\"y\", nw[1])\n        .attr(\"width\", se[0] - nw[0])\n        .attr(\"height\", se[1] - nw[1])\n        .attr(\"preserveAspectRatio\", \"none\")\n        .style(\"pointer-events\", \"none\");\n    }\n\n    const regionPath = svg.append(\"path\")\n      .attr(\"fill\", \"#475569\")\n      .attr(\"fill-opacity\", 0.15)\n      .attr(\"stroke\", \"#475569\")\n      .attr(\"stroke-width\", 1.5)\n      .attr(\"stroke-dasharray\", \"4 4\")\n      .style(\"pointer-events\", \"none\");\n\n    const crosshair = svg.append(\"g\").style(\"pointer-events\", \"none\");\n    crosshair.append(\"circle\")\n      .attr(\"r\", 4).attr(\"fill\", \"none\").attr(\"stroke\", \"#f97316\").attr(\"stroke-width\", 2);\n    crosshair.append(\"line\")\n      .attr(\"x1\", -8).attr(\"x2\", 8).attr(\"y1\", 0).attr(\"y2\", 0)\n      .attr(\"stroke\", \"#f97316\").attr(\"stroke-width\", 2);\n    crosshair.append(\"line\")\n      .attr(\"x1\", 0).attr(\"x2\", 0).attr(\"y1\", -8).attr(\"y2\", 8)\n      .attr(\"stroke\", \"#f97316\").attr(\"stroke-width\", 2);\n\n    const marksLayer = svg.append(\"g\");\n\n    sceneRef.current = { svg, projection, path, regionPath, crosshair, marksLayer };\n\n    return () => {\n      svg.remove();\n      sceneRef.current = null;\n    };\n  }, [basemapImage]);\n\n  React.useEffect(() => {\n    const scene = sceneRef.current;\n    if (!scene) return;\n    const { projection, path, regionPath, crosshair, marksLayer } = scene;\n\n    const circleGeo = d3.geoCircle()\n      .center(target)\n      .radius(radius / 3958.8 * 180 / Math.PI)();\n    regionPath.attr(\"d\", path(circleGeo));\n\n    const [tx, ty] = projection(target);\n    crosshair.attr(\"transform\", `translate(${tx},${ty})`);\n\n    marksLayer.selectAll(\"circle\")\n      .data(shopsWithDist, d => d.shop_id)\n      .join(\"circle\")\n      .attr(\"cx\", d => projection([d.longitude, d.latitude])[0])\n      .attr(\"cy\", d => projection([d.longitude, d.latitude])[1])\n      .attr(\"r\", d => d.distance <= radius ? 5 : 3)\n      .attr(\"fill\", d => d.distance <= radius ? \"#475569\" : \"transparent\")\n      .attr(\"stroke\", d => d.distance <= radius ? \"#f7f0e6\" : \"#4b5563\")\n      .attr(\"stroke-width\", d => d.distance <= radius ? 1.5 : 1)\n      .style(\"cursor\", \"pointer\")\n      .on(\"mouseenter\", (e, d) => {\n        const [px, py] = projection([d.longitude, d.latitude]);\n        setHovered({ shop: d, px, py });\n      })\n      .on(\"mouseleave\", () => setHovered(null));\n  }, [shopsWithDist, radius, target, basemapImage]);\n\n  let ttLeft = 0, ttTop = 0;\n  if (hovered) {\n    ttLeft = hovered.px + 12;\n    ttTop = hovered.py + 12;\n    if (ttLeft + 280 > WIDTH) ttLeft = hovered.px - 292;\n    if (ttTop + 100 > HEIGHT) ttTop = hovered.py - 112;\n  }\n\n  return (\n    <div\n      tabIndex={0}\n      onKeyDown={handleKeyDown}\n      style={{\n        display: \"flex\",\n        width: \"814px\",\n        height: \"548px\",\n        background: \"#f7f0e6\",\n        border: \"2px solid #1a1a1a\",\n        boxSizing: \"border-box\",\n        fontFamily: \"'Space Grotesk', sans-serif\",\n        color: \"#1a1a1a\",\n        outline: \"none\"\n      }}\n    >\n      <div style={{ width: \"472px\", height: \"100%\", position: \"relative\", flexShrink: 0 }}>\n        <div ref={mapRef} style={{ width: \"100%\", height: \"100%\" }} />\n\n        {hovered && (\n          <div style={{\n            position: \"absolute\",\n            left: ttLeft,\n            top: ttTop,\n            background: \"#f7f0e6\",\n            border: \"2px solid #1a1a1a\",\n            boxShadow: \"2px 2px 0 #1a1a1a\",\n            padding: \"12px\",\n            pointerEvents: \"none\",\n            zIndex: 10,\n            width: \"max-content\",\n            maxWidth: \"280px\"\n          }}>\n            <div style={{ fontWeight: 700, fontSize: \"15px\", lineHeight: 1.2, marginBottom: \"4px\" }}>\n              {hovered.shop.name}\n            </div>\n            <div style={{ fontSize: \"15px\", color: \"#4b5563\", marginBottom: \"8px\" }}>\n              {hovered.shop.neighborhood}\n            </div>\n            <div style={{ fontFamily: \"'JetBrains Mono', monospace\", fontSize: \"28px\", color: \"#f97316\", fontWeight: 600 }}>\n              {hovered.shop.distance.toFixed(2)} mi\n            </div>\n          </div>\n        )}\n      </div>\n\n      <div style={{ width: \"2px\", height: \"100%\", background: \"#1a1a1a\", flexShrink: 0 }} />\n\n      <div style={{\n        flex: 1,\n        height: \"548px\",\n        boxSizing: \"border-box\",\n        padding: \"10px 12px\",\n        display: \"flex\",\n        flexDirection: \"column\",\n        overflow: \"hidden\"\n      }}>\n        <SliderRow React={React} value={radius} onChange={setRadius} />\n\n        <div style={{ marginTop: \"8px\", display: \"flex\", flexDirection: \"column\", gap: \"4px\" }}>\n          {inRange.length === 0 ? (\n            <div style={{ fontFamily: \"'JetBrains Mono', monospace\", color: \"#6b7280\", fontSize: \"15px\" }}>\n              No shops in range\n            </div>\n          ) : (\n            inRange.slice(0, 4).map(s => (\n              <div key={s.shop_id} style={{\n                display: \"flex\",\n                justifyContent: \"space-between\",\n                alignItems: \"center\",\n                borderBottom: \"1px dashed #a0a5a8\",\n                paddingBottom: \"4px\"\n              }}>\n                <div style={{ overflow: \"hidden\", paddingRight: \"12px\" }}>\n                  <div style={{ fontWeight: 600, fontSize: \"15px\", whiteSpace: \"nowrap\", overflow: \"hidden\", textOverflow: \"ellipsis\" }}>\n                    {s.name}\n                  </div>\n                  <div style={{ fontSize: \"15px\", color: \"#4b5563\", marginTop: \"2px\" }}>\n                    {s.neighborhood}\n                  </div>\n                </div>\n                <div style={{\n                  fontFamily: \"'JetBrains Mono', monospace\",\n                  fontSize: \"28px\",\n                  color: \"#1a1a1a\",\n                  flexShrink: 0\n                }}>\n                  {s.distance.toFixed(2)} mi\n                </div>\n              </div>\n            ))\n          )}\n        </div>\n      </div>\n    </div>\n  );\n}",
  "outputs": {},
  "inputs_signature": {
    "data": "<input>",
    "basemap_image": "<input>"
  },
  "theme": {
    "name": "vibe-widgets",
    "description": null
  },
  "components": [],
  "model": "google/gemini-3.1-pro-preview",
  "audit": null,
  "save_inputs": {
    "embedded": false,
    "values": {}
  },
  "prompt_history": [
    {
      "prompt": "Houston donut shops.\n\nDATA: the `data` input is a list of shops, one row per shop, with columns: shop_id, name, neighborhood, latitude, longitude, rating (1-5), review_count, price_level (1-3), weekday_wait_min, weekend_wait_min, pumpkin_spice (boolean), open_late (boolean), signature_donut.\nBASEMAP: the `basemap` input is a GeoJSON FeatureCollection giving Houston context, bbox [-95.615, 29.600, -95.255, 29.880]. Each feature has properties.kind: \"bayou\" (LineString), \"loop\" (LineString ring for the Interstate 610 loop), or \"neighborhood\" (Point, with properties.name).\n\nMAP RULES: render the map as one inline SVG with d3 (import * as d3 from \"https://esm.sh/d3@7\"). Use d3.geoMercator with fitExtent on the basemap bbox so the whole bbox is visible inside the map area. Draw in this order: bayous as thin muted lines, the loop as a thin dashed ring, neighborhood names as small uppercase monospace labels in a low-emphasis color, then the shop marks on top. No tile layers, no external map libraries, no network requests at runtime other than the d3 import. Distances are great-circle miles from the haversine formula. Position the tooltip inside the map bounds.\n\nLAYOUT RULES: the widget is exactly 814x548 pixels, background #f2f0e9, no scrollbars, no responsiveness to window size. Fonts: 'Space Grotesk', sans-serif for text and 'JetBrains Mono', monospace for numbers, labels and small caps; both are provided by the host, do not load fonts.\n\nCOPY RULES (strict): no title or heading, no explanatory paragraphs, no instructions longer than one short line, no code or SQL readouts, no placeholder text, no emoji, no decorative badges, no record counts unless they are the result of the interaction. Every number shown is computed from the data.\n\nQuery parameterization: the interaction sets the parameters of a fixed query (a center and a radius).\nTwo-column layout: the map fills the left 58% of the width at full height; a control panel fills the right side; a 2px #1a1a1a vertical rule separates them.\nThe search region is a circle around a target. The target is a crosshair marker; dragging anywhere on the map moves it (pointer drag with live updates) and arrow keys nudge it. The radius comes from a slider in the panel (0.25 to 4 miles, step 0.05, default 0.8) whose current value is shown beside it as e.g. \"0.80 mi\". Draw the region as a circle filled #3f8bdb at low opacity with a dashed #3f8bdb outline. Shops inside the region are filled #3f8bdb with a white outline; shops outside are small muted hollow circles.\nPanel, top to bottom: the radius slider row; four stat tiles in a 2x2 grid computed from the shops inside the region: shops in range (count), mean rating (two decimals), mean weekend wait (rounded minutes), most common price level shown as $ signs; then a ranked list of up to five shops inside the region sorted by distance, each with name, neighborhood, and distance in miles (two decimals). If no shop is inside, the list is one short line: \"No shops in range\". Hover on a shop shows a card tooltip with name, neighborhood, distance to the target.\n\nREVISION: Bug fix only. The map collapses every mark to a single point because the projection is fitted with fitExtent on a Polygon built from the bbox; d3-geo reads that ring's winding as the whole sphere. Replace that polygon with a MultiPoint of the two bbox corners: fitExtent(extent, { type: \"MultiPoint\", coordinates: [[-95.615, 29.600], [-95.255, 29.880]] }). Keep every other line, style, label and interaction exactly as it is.\n\nREVISION: Default state fix only. The target starts at [-95.435, 29.740], far from every shop, so the widget opens with zero shops in range. Start the target at longitude -95.3862, latitude 29.7452 instead (between Montrose and Midtown) so several shops are inside the default 0.80 mile region on load. Change nothing else.\n\nREVISION: Two changes to make everywhere in this file, on top of whatever else this request asks for:\n\n1) REAL BASEMAP. A new input `basemap_image` is provided: a data: URL (JPEG) of a real, already-styled Houston street map -- roads, water, and neighborhood names are baked into the image itself. Its corners are NW corner [lon -95.615, lat 29.880], SE corner [lon -95.255, lat 29.600]. Read it with model.get(\"basemap_image\"). Render it FIRST, as a single SVG <image href={basemapImage}>: project the two bbox corners above with the SAME d3 projection used for the shop marks to get its on-screen rectangle (x, y, width, height from the two projected points), and set preserveAspectRatio=\"none\" so it exactly fills that rectangle -- this is what keeps the image aligned with the marks. Then DELETE every bit of code that draws your own roads, water, bayous, the 610 loop, neighborhood-name text labels, a background grid, or any bounding/frame circle around the whole map -- the image already shows all of that, and drawing your own on top would duplicate and clash with it. If the `basemap` GeoJSON input still exists in the code, remove every use of it along with any now-dead helper code, styles, or legend entries that only existed to describe those removed line-drawn elements. Keep every shop mark, overlay (target, search region, brush, connecting lines), tooltip and panel exactly as they are, just drawn on top of the new image instead of the old lines.\n\n2) POINTER MATH SAFETY. This widget renders inside an iframe inside a scaled presentation slide, which breaks two common shortcuts. Find every place in this file that converts a mouse/pointer position into a map coordinate and fix it if needed: NEVER use d3.drag's `event.x`/`event.y`, and NEVER use `d3.pointer(event, node)` -- both rely on SVGElement.getScreenCTM(), which does not account for an ancestor iframe living inside a CSS-transformed (scaled) page and silently returns the wrong offset there, producing a drag that visibly lags/jumps away from the cursor. Instead, always compute the position by hand: read the svg element's own `getBoundingClientRect()` and subtract its `left`/`top` from the event's `clientX`/`clientY` (inside a d3.drag `.on(\"drag\", ...)` handler, read those off `event.sourceEvent.clientX`/`event.sourceEvent.clientY`, not off `event.x`/`event.y`). If there is a rectangular brush/marquee-select interaction implemented with d3.brush(), replace it with a hand-rolled version using plain pointerdown/pointermove/pointerup listeners and an SVG <rect> you draw yourself, computing every corner the same getBoundingClientRect way -- d3.brush() has the identical getScreenCTM problem internally. Keep the resulting interaction pixel-identical to how it behaves today; this is a coordinate-math fix only, not a redesign.\n\nREVISION: Legibility only -- this widget is projected on a large screen from the back of a conference room, so nothing about its behaviour, layout or colours should change.\n\nRaise every piece of text in the widget to at least 15px. Any number that is the result of the interaction -- a count, a mean, a headline figure -- goes to at least 28px. Tooltip text 15px. Axis tick labels 14px and axis titles 15px, with at most four ticks on each axis. Legend and button labels 15px.\n\nWhere a label is only decoration and is not needed to read the result, remove it rather than shrink anything else to make room. Do not add any new text.\n\nChange nothing else: same marks, same colours, same interactions, same panel structure, same computed values.\n\nREVISION: Layout fix only. After the last change the widget's content is 555px tall, but the widget is exactly 814x548 and anything past that is clipped, so the bottom of the right-hand panel is being cut off.\n\nMake the whole widget fit inside 548px of height again, with nothing clipped and no scrollbar. Do it by tightening vertical spacing in the right-hand panel -- reduce the gaps between the stat tiles, the ranked list rows and the slider row, and trim the panel's outer padding -- and if that is still not enough, show four ranked shops instead of five.\n\nDo NOT reduce any font size to make room: every piece of text must stay at least 15px, and the stat-tile numbers must stay at least 28px. Change nothing else: same map, same marks, same drag and slider behaviour, same computed values.\n\nREVISION: Layout fix only, and the previous attempt did not work -- the content is still 555px tall inside a widget that is exactly 814 x 548, so the bottom 7px is clipped off.\n\nFind the outermost container of the right-hand panel and make the whole right column a flex column with a FIXED height of 548px, boxSizing 'border-box', overflow 'hidden', and its vertical padding reduced to 10px top and bottom. Then remove roughly 10px of vertical space from inside it: cut the gaps between the slider row, the 2x2 stat tile grid and the ranked list, and cut the padding inside each ranked-list row. If after that the content still does not fit, render four ranked shops instead of five.\n\nDo not change any font size: all text stays at 15px or larger and the stat-tile numbers stay at 28px or larger. Do not change the map, the marks, the drag behaviour, the slider, or any computed value.",
      "source": "create"
    }
  ]
}