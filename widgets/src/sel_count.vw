{
  "version": "1.0",
  "created_at": "2026-09-16T10:52:32",
  "description": "Houston donut shops.\n\nDATA: the `data` input is a list of shops, one row per shop, with columns: shop_id, name, neighborhood, latitude, longitude, rating (1-5), review_count, price_level (1-3), weekday_wait_min, weekend_wait_min, pumpkin_spice (boolean), open_late (boolean), signature_donut.\n\nPOINTER RULES (do this exactly -- the widget renders inside an iframe inside a scaled presentation slide, which breaks the usual shortcuts): whenever you convert a mouse/pointer position into a screen coordinate (dragging a marker, brushing a selection, positioning a tooltip), NEVER use d3.drag's `event.x`/`event.y`, and NEVER use `d3.pointer(event, node)`. Both rely on SVGElement.getScreenCTM(), which does not account for an ancestor iframe living inside a CSS-transformed (scaled) page and silently returns the wrong offset there. Instead, always compute the position by hand: get the svg element's own `getBoundingClientRect()` once (cache it in a ref, refresh on resize) and subtract its `left`/`top` from the raw event's `clientX`/`clientY` (for a d3.drag handler, read them off `event.sourceEvent.clientX`/`event.sourceEvent.clientY`). If a rectangular brush/marquee-select interaction is needed, implement it by hand with plain pointerdown/pointermove/pointerup listeners and an SVG <rect> you draw yourself (computing corners the same getBoundingClientRect way) -- do not use d3.brush(), which has the identical getScreenCTM problem internally.\n\nLAYOUT RULES: the widget is exactly 352x400 pixels, background #f2f0e9, no scrollbars, no responsiveness to window size. Fonts: 'Space Grotesk', sans-serif for text and 'JetBrains Mono', monospace for numbers, labels and small caps; both are provided by the host, do not load fonts.\n\nCOPY RULES (strict): no title or heading, no explanatory paragraphs, no instructions longer than one short line, no code or SQL readouts, no placeholder text, no emoji, no decorative badges, no record counts unless they are the result of the interaction. Every number shown is computed from the data.\n\nAll three widgets on this slide draw the same chart and must match each other exactly: a scatter plot of the shops, x axis review_count on a LINEAR scale (not log) starting at 0, y axis weekend_wait_min on a linear scale. One circle per shop, radius 5, with a thin #1a1a1a outline. Plain axes with a handful of ticks and small monospace tick labels, short axis titles \"REVIEWS\" and \"WEEKEND WAIT / MIN\" in small uppercase monospace, and enough room at the left and bottom for them. No gridlines, no trend line, no legend, no annotation layer.\nOne shop has several times everyone else's review count, so by default the other shops crowd into the left third of the plot. That is intentional -- do not clip it, drop it, or switch the axis to a log scale to hide it.\nSelection constructs a new element: the selection becomes a mark on the chart, and that mark carries numbers the chart never showed.\nDrag a rectangular region anywhere over the plot using plain pointer events (pointerdown, pointermove, pointerup) and an SVG rect you draw yourself. While dragging and after release the region stays drawn, filled #ea580c at low opacity with a 2px dashed #ea580c outline. Dragging again starts a new region; a click without movement clears it. The axes never rescale.\nPinned to the region's top-left corner and moving with it -- part of the region, never a side panel -- draw a square-cornered summary card about 132px wide (bone fill, 2px #1a1a1a border, hard 3px offset shadow) containing, top to bottom: the number of shops inside the region in large monospace type, with \"shops\" (or \"shop\" when the count is exactly 1) in small uppercase monospace under it; a thin divider; then two compact comparison rows, one for mean weekend wait in minutes to one decimal and one for mean review count rounded to a whole number, each row labelled in small uppercase monospace and giving the value inside the region, the value for every shop outside it, and the signed difference between them. If nothing is inside the region the comparison rows read a dash. The card keeps itself inside the plot area.\nCircles inside the region are filled #ea580c with a white outline; the rest are filled #cbd5e1 at fill-opacity 0.7. With no region at all, every circle is filled #6b7280 at fill-opacity 0.8.\nOne short line in the bottom-left corner: \"drag a region\". Nothing else.\n\nREVISION: Layout fix only -- do not change any interaction, colour, label or behaviour. The circle for the shop with the largest review count is currently drawn half outside the right edge of the plot and gets clipped. Pad the x scale so every circle is fully inside the drawing area: extend the x domain past the largest review count by enough that the rightmost circle plus its outline clears the edge (round the domain maximum up to a sensible tick value), and make sure the plot area leaves a few pixels of room on the right. Do the same check on the y scale so the topmost and bottommost circles are not clipped either. Two more layout fixes in the same pass. (1) The summary card attached to the region is far too large for a 352px-wide widget -- it covers most of the chart. Shrink it to about 128px wide: the count in roughly 20px monospace, the word under it and the IN / OUT / delta comparison rows in roughly 8px monospace, and tighter padding throughout. Keep every number, row and label it shows now, and keep it pinned to the region corner. (2) The bottom-left hint line \"drag a region\" currently overlaps the \"REVIEWS\" axis title -- move the hint to the top-left corner inside the plot area instead, so nothing overlaps.\n\nREVISION: Three changes.\n\n(1) Exclude the one shop whose review_count is above 3000 from this chart entirely -- do not draw it and do not count it in any scale or any statistic -- so the x axis fits the remaining shops and they spread across the full plot width.\n\n(2) Move the summary card so it sits ABOVE the drawn region, horizontally centred on it, and follows the region as it is drawn and redrawn; if there is not enough room above, put it directly below the region instead. It must stay inside the plot area. Replace its contents with exactly three lines of 'JetBrains Mono' monospace, and nothing else:\n  - the count, as \"7 shops\" (or \"1 shop\"), at 28px;\n  - a wait row: a small solid SVG triangle followed by the signed difference between the mean weekend wait inside the region and the mean weekend wait outside it, to one decimal, as \"+6.2 min\" or \"-3.4 min\", at 16px;\n  - a reviews row: the same shape, for mean review count, rounded to a whole number, as \"+210 reviews\" or \"-85 reviews\", at 16px.\nThe triangle points up and is filled #ea580c when the inside value is higher than the outside value, and points down and is filled #3f8bdb when it is lower. Do not show the inside value and the outside value themselves, and do not label the rows with words like \"inside\" or \"outside\" -- the arrow and the signed delta are the whole readout. When no shop is inside the region, the two rows read a dash with no triangle.\n\n(3) Legibility, because this is projected: axis tick labels 14px, axis titles 15px, at most four ticks on each axis, and remove the \"drag a region\" instruction line entirely.\n\nEverything else stays exactly as it is: dragging with plain pointer events, the region staying drawn after release, a click without movement clearing it, the axes never rescaling, and the same mark colours.",
  "code": "import * as d3 from \"https://esm.sh/d3@7\";\n\nexport const SummaryCard = ({ box, selected, unselected, React }) => {\n  if (!box) return null;\n\n  const minX = Math.min(box.x1, box.x2);\n  const minY = Math.min(box.y1, box.y2);\n  const maxX = Math.max(box.x1, box.x2);\n  const maxY = Math.max(box.y1, box.y2);\n\n  if (maxX - minX < 3 && maxY - minY < 3) return null;\n\n  const cardW = 180;\n  const cardH = 92;\n  \n  const midX = minX + (maxX - minX) / 2;\n  let left = midX - cardW / 2;\n  left = Math.max(50, Math.min(left, 332 - cardW));\n\n  let top = minY - cardH - 12;\n  if (top < 20) {\n    top = maxY + 12;\n  }\n  top = Math.max(20, Math.min(top, 350 - cardH));\n\n  const hasSel = selected.length > 0;\n  const hasUnsel = unselected.length > 0;\n\n  const inWait = hasSel ? selected.reduce((s, d) => s + d.weekend_wait_min, 0) / selected.length : NaN;\n  const outWait = hasUnsel ? unselected.reduce((s, d) => s + d.weekend_wait_min, 0) / unselected.length : NaN;\n  const inRev = hasSel ? selected.reduce((s, d) => s + d.review_count, 0) / selected.length : NaN;\n  const outRev = hasUnsel ? unselected.reduce((s, d) => s + d.review_count, 0) / unselected.length : NaN;\n\n  const waitDiff = inWait - outWait;\n  const revDiff = inRev - outRev;\n\n  const renderRow = (diff, unit, decimals) => {\n    if (!hasSel || isNaN(diff)) {\n      return <div style={{ fontSize: 16, lineHeight: '22px' }}>-</div>;\n    }\n    \n    const isUp = diff > 0;\n    const isDown = diff < 0;\n    const color = isUp ? '#ea580c' : (isDown ? '#3f8bdb' : '#1a1a1a');\n    \n    const valStr = Math.abs(diff).toFixed(decimals);\n    const displayVal = (isUp ? \"+\" : (isDown ? \"-\" : \"\")) + valStr;\n\n    return (\n      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', fontSize: 16, lineHeight: '22px' }}>\n        {isUp || isDown ? (\n          <svg width=\"12\" height=\"12\" viewBox=\"0 0 12 12\" style={{ flexShrink: 0 }}>\n            {isUp ? (\n              <polygon points=\"1,9 11,9 6,2\" fill={color} />\n            ) : (\n              <polygon points=\"1,3 11,3 6,10\" fill={color} />\n            )}\n          </svg>\n        ) : <div style={{ width: 12, height: 12 }} />}\n        <span>{displayVal} {unit}</span>\n      </div>\n    );\n  };\n\n  return (\n    <div style={{\n      position: 'absolute',\n      left, top,\n      width: cardW,\n      backgroundColor: '#f7f0e6',\n      border: '2px solid #1a1a1a',\n      boxShadow: '3px 3px 0px #1a1a1a',\n      padding: '8px 12px',\n      boxSizing: 'border-box',\n      pointerEvents: 'none',\n      display: 'flex',\n      flexDirection: 'column',\n      justifyContent: 'center',\n      fontFamily: 'JetBrains Mono, monospace',\n      color: '#1a1a1a',\n      whiteSpace: 'nowrap'\n    }}>\n      <div style={{ fontSize: 28, fontWeight: 'bold', lineHeight: '32px', marginBottom: 4 }}>\n        {selected.length} {selected.length === 1 ? 'shop' : 'shops'}\n      </div>\n      {renderRow(waitDiff, 'min', 1)}\n      {renderRow(revDiff, 'reviews', 0)}\n    </div>\n  );\n};\n\nexport default function Widget({ model, React }) {\n  const [data, setData] = React.useState(() => model.get(\"data\") || []);\n  \n  React.useEffect(() => {\n    const handler = () => setData(model.get(\"data\") || []);\n    model.on(\"change:data\", handler);\n    return () => model.off(\"change:data\", handler);\n  }, [model]);\n\n  const parsedData = React.useMemo(() => {\n    return data\n      .map(d => ({\n        ...d,\n        review_count: Number(d.review_count),\n        weekend_wait_min: Number(d.weekend_wait_min)\n      }))\n      .filter(d => d.review_count <= 3000);\n  }, [data]);\n\n  const svgRef = React.useRef(null);\n  const [box, setBox] = React.useState(null);\n  const [isDragging, setIsDragging] = React.useState(false);\n\n  const xMax = React.useMemo(() => d3.max(parsedData, d => d.review_count) || 100, [parsedData]);\n  const yMax = React.useMemo(() => d3.max(parsedData, d => d.weekend_wait_min) || 100, [parsedData]);\n  \n  const xScale = React.useMemo(() => d3.scaleLinear().domain([0, xMax * 1.05]).nice().range([50, 332]), [xMax]);\n  const yScale = React.useMemo(() => d3.scaleLinear().domain([0, yMax * 1.05]).nice().range([350, 20]), [yMax]);\n\n  const handlePointerDown = React.useCallback((e) => {\n    if (!svgRef.current) return;\n    const rect = svgRef.current.getBoundingClientRect();\n    const x = e.clientX - rect.left;\n    const y = e.clientY - rect.top;\n    setBox({ x1: x, y1: y, x2: x, y2: y });\n    setIsDragging(true);\n    e.target.setPointerCapture(e.pointerId);\n  }, []);\n\n  const handlePointerMove = React.useCallback((e) => {\n    if (!isDragging || !svgRef.current) return;\n    const rect = svgRef.current.getBoundingClientRect();\n    const x = e.clientX - rect.left;\n    const y = e.clientY - rect.top;\n    setBox(prev => ({ ...prev, x2: x, y2: y }));\n  }, [isDragging]);\n\n  const handlePointerUp = React.useCallback((e) => {\n    if (!isDragging) return;\n    setIsDragging(false);\n    e.target.releasePointerCapture(e.pointerId);\n    if (box && Math.abs(box.x1 - box.x2) < 3 && Math.abs(box.y1 - box.y2) < 3) {\n      setBox(null);\n    }\n  }, [isDragging, box]);\n\n  const minX = box ? Math.min(box.x1, box.x2) : 0;\n  const maxX = box ? Math.max(box.x1, box.x2) : 0;\n  const minY = box ? Math.min(box.y1, box.y2) : 0;\n  const maxY = box ? Math.max(box.y1, box.y2) : 0;\n  const hasBox = box && (maxX - minX >= 3 || maxY - minY >= 3);\n\n  const selected = [];\n  const unselected = [];\n  \n  parsedData.forEach(d => {\n    const cx = xScale(d.review_count);\n    const cy = yScale(d.weekend_wait_min);\n    if (hasBox && cx >= minX && cx <= maxX && cy >= minY && cy <= maxY) {\n      selected.push(d);\n    } else {\n      unselected.push(d);\n    }\n  });\n\n  return (\n    <div style={{ width: 352, height: 400, backgroundColor: '#f7f0e6', position: 'relative', overflow: 'hidden', userSelect: 'none' }}>\n      <svg\n        ref={svgRef}\n        width={352} height={400}\n        style={{ position: 'absolute', top: 0, left: 0, touchAction: 'none' }}\n        onPointerDown={handlePointerDown}\n        onPointerMove={handlePointerMove}\n        onPointerUp={handlePointerUp}\n        onPointerCancel={handlePointerUp}\n      >\n        <line x1={50} x2={332} y1={350} y2={350} stroke=\"#1a1a1a\" />\n        <line x1={50} x2={50} y1={20} y2={350} stroke=\"#1a1a1a\" />\n\n        {xScale.ticks(4).map(tick => (\n          <g key={`x-${tick}`} transform={`translate(${xScale(tick)}, 350)`}>\n            <line y2={4} stroke=\"#1a1a1a\" />\n            <text y={20} textAnchor=\"middle\" fontSize={14} fontFamily=\"JetBrains Mono, monospace\" fill=\"#1a1a1a\">{tick}</text>\n          </g>\n        ))}\n        {yScale.ticks(4).map(tick => (\n          <g key={`y-${tick}`} transform={`translate(50, ${yScale(tick)})`}>\n            <line x2={-4} stroke=\"#1a1a1a\" />\n            <text x={-8} dominantBaseline=\"middle\" textAnchor=\"end\" fontSize={14} fontFamily=\"JetBrains Mono, monospace\" fill=\"#1a1a1a\">{tick}</text>\n          </g>\n        ))}\n\n        <text x={50} y={392} fontSize={15} fontFamily=\"JetBrains Mono, monospace\" fill=\"#1a1a1a\" fontWeight=\"bold\">REVIEWS</text>\n        <text x={14} y={350} transform=\"rotate(-90, 14, 350)\" fontSize={15} fontFamily=\"JetBrains Mono, monospace\" fill=\"#1a1a1a\" fontWeight=\"bold\">WEEKEND WAIT / MIN</text>\n\n        {parsedData.map((d, i) => {\n          const cx = xScale(d.review_count);\n          const cy = yScale(d.weekend_wait_min);\n          const isSel = hasBox && cx >= minX && cx <= maxX && cy >= minY && cy <= maxY;\n          const fill = !hasBox ? '#6b7280' : (isSel ? '#ea580c' : '#cbd5e1');\n          const op = !hasBox ? 0.8 : (isSel ? 1 : 0.7);\n          const str = !hasBox ? '#1a1a1a' : (isSel ? 'white' : '#1a1a1a');\n          \n          return (\n            <circle \n              key={i} cx={cx} cy={cy} r={5} \n              fill={fill} fillOpacity={op} \n              stroke={str} strokeWidth={1} \n              pointerEvents=\"none\" \n            />\n          );\n        })}\n\n        {hasBox && (\n          <rect\n            x={minX} y={minY} width={maxX - minX} height={maxY - minY}\n            fill=\"#ea580c\" fillOpacity={0.15}\n            stroke=\"#ea580c\" strokeWidth={2} strokeDasharray=\"4 4\"\n            pointerEvents=\"none\"\n          />\n        )}\n      </svg>\n\n      <SummaryCard box={hasBox ? box : null} selected={selected} unselected={unselected} React={React} />\n    </div>\n  );\n}",
  "outputs": {},
  "inputs_signature": {
    "data": "<input>"
  },
  "theme": {
    "name": "vibe-widgets",
    "description": null
  },
  "components": [],
  "model": "google/gemini-3.1-pro-preview",
  "audit": null,
  "save_inputs": {
    "embedded": false,
    "values": {}
  },
  "prompt_history": [
    {
      "prompt": "Houston donut shops.\n\nDATA: the `data` input is a list of shops, one row per shop, with columns: shop_id, name, neighborhood, latitude, longitude, rating (1-5), review_count, price_level (1-3), weekday_wait_min, weekend_wait_min, pumpkin_spice (boolean), open_late (boolean), signature_donut.\n\nPOINTER RULES (do this exactly -- the widget renders inside an iframe inside a scaled presentation slide, which breaks the usual shortcuts): whenever you convert a mouse/pointer position into a screen coordinate (dragging a marker, brushing a selection, positioning a tooltip), NEVER use d3.drag's `event.x`/`event.y`, and NEVER use `d3.pointer(event, node)`. Both rely on SVGElement.getScreenCTM(), which does not account for an ancestor iframe living inside a CSS-transformed (scaled) page and silently returns the wrong offset there. Instead, always compute the position by hand: get the svg element's own `getBoundingClientRect()` once (cache it in a ref, refresh on resize) and subtract its `left`/`top` from the raw event's `clientX`/`clientY` (for a d3.drag handler, read them off `event.sourceEvent.clientX`/`event.sourceEvent.clientY`). If a rectangular brush/marquee-select interaction is needed, implement it by hand with plain pointerdown/pointermove/pointerup listeners and an SVG <rect> you draw yourself (computing corners the same getBoundingClientRect way) -- do not use d3.brush(), which has the identical getScreenCTM problem internally.\n\nLAYOUT RULES: the widget is exactly 352x400 pixels, background #f2f0e9, no scrollbars, no responsiveness to window size. Fonts: 'Space Grotesk', sans-serif for text and 'JetBrains Mono', monospace for numbers, labels and small caps; both are provided by the host, do not load fonts.\n\nCOPY RULES (strict): no title or heading, no explanatory paragraphs, no instructions longer than one short line, no code or SQL readouts, no placeholder text, no emoji, no decorative badges, no record counts unless they are the result of the interaction. Every number shown is computed from the data.\n\nAll three widgets on this slide draw the same chart and must match each other exactly: a scatter plot of the shops, x axis review_count on a LINEAR scale (not log) starting at 0, y axis weekend_wait_min on a linear scale. One circle per shop, radius 5, with a thin #1a1a1a outline. Plain axes with a handful of ticks and small monospace tick labels, short axis titles \"REVIEWS\" and \"WEEKEND WAIT / MIN\" in small uppercase monospace, and enough room at the left and bottom for them. No gridlines, no trend line, no legend, no annotation layer.\nOne shop has several times everyone else's review count, so by default the other shops crowd into the left third of the plot. That is intentional -- do not clip it, drop it, or switch the axis to a log scale to hide it.\nSelection constructs a new element: the selection becomes a mark on the chart, and that mark carries numbers the chart never showed.\nDrag a rectangular region anywhere over the plot using plain pointer events (pointerdown, pointermove, pointerup) and an SVG rect you draw yourself. While dragging and after release the region stays drawn, filled #ea580c at low opacity with a 2px dashed #ea580c outline. Dragging again starts a new region; a click without movement clears it. The axes never rescale.\nPinned to the region's top-left corner and moving with it -- part of the region, never a side panel -- draw a square-cornered summary card about 132px wide (bone fill, 2px #1a1a1a border, hard 3px offset shadow) containing, top to bottom: the number of shops inside the region in large monospace type, with \"shops\" (or \"shop\" when the count is exactly 1) in small uppercase monospace under it; a thin divider; then two compact comparison rows, one for mean weekend wait in minutes to one decimal and one for mean review count rounded to a whole number, each row labelled in small uppercase monospace and giving the value inside the region, the value for every shop outside it, and the signed difference between them. If nothing is inside the region the comparison rows read a dash. The card keeps itself inside the plot area.\nCircles inside the region are filled #ea580c with a white outline; the rest are filled #cbd5e1 at fill-opacity 0.7. With no region at all, every circle is filled #6b7280 at fill-opacity 0.8.\nOne short line in the bottom-left corner: \"drag a region\". Nothing else.\n\nREVISION: Layout fix only -- do not change any interaction, colour, label or behaviour. The circle for the shop with the largest review count is currently drawn half outside the right edge of the plot and gets clipped. Pad the x scale so every circle is fully inside the drawing area: extend the x domain past the largest review count by enough that the rightmost circle plus its outline clears the edge (round the domain maximum up to a sensible tick value), and make sure the plot area leaves a few pixels of room on the right. Do the same check on the y scale so the topmost and bottommost circles are not clipped either. Two more layout fixes in the same pass. (1) The summary card attached to the region is far too large for a 352px-wide widget -- it covers most of the chart. Shrink it to about 128px wide: the count in roughly 20px monospace, the word under it and the IN / OUT / delta comparison rows in roughly 8px monospace, and tighter padding throughout. Keep every number, row and label it shows now, and keep it pinned to the region corner. (2) The bottom-left hint line \"drag a region\" currently overlaps the \"REVIEWS\" axis title -- move the hint to the top-left corner inside the plot area instead, so nothing overlaps.\n\nREVISION: Three changes.\n\n(1) Exclude the one shop whose review_count is above 3000 from this chart entirely -- do not draw it and do not count it in any scale or any statistic -- so the x axis fits the remaining shops and they spread across the full plot width.\n\n(2) Move the summary card so it sits ABOVE the drawn region, horizontally centred on it, and follows the region as it is drawn and redrawn; if there is not enough room above, put it directly below the region instead. It must stay inside the plot area. Replace its contents with exactly three lines of 'JetBrains Mono' monospace, and nothing else:\n  - the count, as \"7 shops\" (or \"1 shop\"), at 28px;\n  - a wait row: a small solid SVG triangle followed by the signed difference between the mean weekend wait inside the region and the mean weekend wait outside it, to one decimal, as \"+6.2 min\" or \"-3.4 min\", at 16px;\n  - a reviews row: the same shape, for mean review count, rounded to a whole number, as \"+210 reviews\" or \"-85 reviews\", at 16px.\nThe triangle points up and is filled #ea580c when the inside value is higher than the outside value, and points down and is filled #3f8bdb when it is lower. Do not show the inside value and the outside value themselves, and do not label the rows with words like \"inside\" or \"outside\" -- the arrow and the signed delta are the whole readout. When no shop is inside the region, the two rows read a dash with no triangle.\n\n(3) Legibility, because this is projected: axis tick labels 14px, axis titles 15px, at most four ticks on each axis, and remove the \"drag a region\" instruction line entirely.\n\nEverything else stays exactly as it is: dragging with plain pointer events, the region staying drawn after release, a click without movement clearing it, the axes never rescaling, and the same mark colours.",
      "source": "create"
    }
  ]
}