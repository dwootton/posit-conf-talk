{
  "version": "1.0",
  "created_at": "2026-09-14T21:05:28",
  "description": "Houston donut shops.\n\nDATA: the `data` input is a list of shops, one row per shop, with columns: shop_id, name, neighborhood, latitude, longitude, rating (1-5), review_count, price_level (1-3), weekday_wait_min, weekend_wait_min, pumpkin_spice (boolean), open_late (boolean), signature_donut.\nBASEMAP: the `basemap` input is a GeoJSON FeatureCollection giving Houston context, bbox [-95.615, 29.600, -95.255, 29.880]. Each feature has properties.kind: \"bayou\" (LineString), \"loop\" (LineString ring for the Interstate 610 loop), or \"neighborhood\" (Point, with properties.name).\n\nMAP RULES: render the map as one inline SVG with d3 (import * as d3 from \"https://esm.sh/d3@7\"). Use d3.geoMercator with fitExtent on the basemap bbox so the whole bbox is visible inside the map area. Draw in this order: bayous as thin muted lines, the loop as a thin dashed ring, neighborhood names as small uppercase monospace labels in a low-emphasis color, then the shop marks on top. No tile layers, no external map libraries, no network requests at runtime other than the d3 import. Distances are great-circle miles from the haversine formula. Position the tooltip inside the map bounds.\n\nLAYOUT RULES: the widget is exactly 352x400 pixels, background #f2f0e9, no scrollbars, no responsiveness to window size. Fonts: 'Space Grotesk', sans-serif for text and 'JetBrains Mono', monospace for numbers, labels and small caps; both are provided by the host, do not load fonts.\n\nCOPY RULES (strict): no title or heading, no explanatory paragraphs, no instructions longer than one short line, no code or SQL readouts, no placeholder text, no emoji, no decorative badges, no record counts unless they are the result of the interaction. Every number shown is computed from the data.\n\nHypothesis: where is close?\nPlace a \"you are here\" marker at longitude -95.3848, latitude 29.7468, drawn as a white circle with a #1a1a1a outline and a dark center dot. Draw 1 and 2 mile rings as thin dashed circles around it. Draw every shop as a circle: the five nearest shops by haversine distance are filled #6b3fa0 with a white outline, connected to the marker by a thin #6b3fa0 line and labeled with shop name and distance in miles (two decimals); every other shop is a small muted hollow circle. The marker is draggable anywhere on the map (pointer events, live recompute while dragging). Hover on a shop shows a card tooltip with name, neighborhood, distance in miles.\n\nREVISION: Bug fix only. The map collapses every mark to a single point because the projection is fitted with fitExtent on a Polygon built from the bbox; d3-geo reads that ring's winding as the whole sphere. Replace that polygon with a MultiPoint of the two bbox corners: fitExtent(extent, { type: \"MultiPoint\", coordinates: [[-95.615, 29.600], [-95.255, 29.880]] }). Keep every other line, style, label and interaction exactly as it is.\n\nREVISION: Label placement fix only. The five nearest-shop labels (name and distance) pile on top of each other because the nearest shops cluster together. Place each label with a greedy search over candidate positions (right, left, above, below of its mark, then the same positions one label-height further out, with a 4px gap) and skip any label whose box would intersect an already placed label or leave the SVG. Keep the label text to the shop name on one line and the distance on a second smaller line, and give the text a 3px background-colored halo (paint-order: stroke). Change nothing else.\n\nREVISION: Two changes to make everywhere in this file, on top of whatever else this request asks for:\n\n1) REAL BASEMAP. A new input `basemap_image` is provided: a data: URL (JPEG) of a real, already-styled Houston street map -- roads, water, and neighborhood names are baked into the image itself. Its corners are NW corner [lon -95.615, lat 29.880], SE corner [lon -95.255, lat 29.600]. Read it with model.get(\"basemap_image\"). Render it FIRST, as a single SVG <image href={basemapImage}>: project the two bbox corners above with the SAME d3 projection used for the shop marks to get its on-screen rectangle (x, y, width, height from the two projected points), and set preserveAspectRatio=\"none\" so it exactly fills that rectangle -- this is what keeps the image aligned with the marks. Then DELETE every bit of code that draws your own roads, water, bayous, the 610 loop, neighborhood-name text labels, a background grid, or any bounding/frame circle around the whole map -- the image already shows all of that, and drawing your own on top would duplicate and clash with it. If the `basemap` GeoJSON input still exists in the code, remove every use of it along with any now-dead helper code, styles, or legend entries that only existed to describe those removed line-drawn elements. Keep every shop mark, overlay (target, search region, brush, connecting lines), tooltip and panel exactly as they are, just drawn on top of the new image instead of the old lines.\n\n2) POINTER MATH SAFETY. This widget renders inside an iframe inside a scaled presentation slide, which breaks two common shortcuts. Find every place in this file that converts a mouse/pointer position into a map coordinate and fix it if needed: NEVER use d3.drag's `event.x`/`event.y`, and NEVER use `d3.pointer(event, node)` -- both rely on SVGElement.getScreenCTM(), which does not account for an ancestor iframe living inside a CSS-transformed (scaled) page and silently returns the wrong offset there, producing a drag that visibly lags/jumps away from the cursor. Instead, always compute the position by hand: read the svg element's own `getBoundingClientRect()` and subtract its `left`/`top` from the event's `clientX`/`clientY` (inside a d3.drag `.on(\"drag\", ...)` handler, read those off `event.sourceEvent.clientX`/`event.sourceEvent.clientY`, not off `event.x`/`event.y`). If there is a rectangular brush/marquee-select interaction implemented with d3.brush(), replace it with a hand-rolled version using plain pointerdown/pointermove/pointerup listeners and an SVG <rect> you draw yourself, computing every corner the same getBoundingClientRect way -- d3.brush() has the identical getScreenCTM problem internally. Keep the resulting interaction pixel-identical to how it behaves today; this is a coordinate-math fix only, not a redesign.",
  "code": "import * as d3 from \"https://esm.sh/d3@7\";\n\nfunction haversine(lon1, lat1, lon2, lat2) {\n  const R = 3958.8;\n  const dLat = (lat2 - lat1) * Math.PI / 180;\n  const dLon = (lon2 - lon1) * Math.PI / 180;\n  const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +\n            Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *\n            Math.sin(dLon / 2) * Math.sin(dLon / 2);\n  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));\n  return R * c;\n}\n\nexport default function Widget({ model, React }) {\n  const [data, setData] = React.useState(model.get(\"data\") || []);\n  const [basemapImage, setBasemapImage] = React.useState(model.get(\"basemap_image\") || \"\");\n\n  const [markerPos, setMarkerPos] = React.useState([-95.3848, 29.7468]);\n  const [isDragging, setIsDragging] = React.useState(false);\n  const [hoveredShop, setHoveredShop] = React.useState(null);\n  const svgRef = React.useRef(null);\n\n  React.useEffect(() => {\n    const onDataChange = () => setData(model.get(\"data\") || []);\n    const onBasemapChange = () => setBasemapImage(model.get(\"basemap_image\") || \"\");\n\n    model.on(\"change:data\", onDataChange);\n    model.on(\"change:basemap_image\", onBasemapChange);\n\n    return () => {\n      model.off(\"change:data\", onDataChange);\n      model.off(\"change:basemap_image\", onBasemapChange);\n    };\n  }, [model]);\n\n  const { projection, imgX, imgY, imgW, imgH, oneMilePx } = React.useMemo(() => {\n    const width = 352;\n    const height = 400;\n    const nw = [-95.615, 29.880];\n    const se = [-95.255, 29.600];\n    const bboxGeoJSON = {\n      type: \"MultiPoint\",\n      coordinates: [nw, se]\n    };\n    \n    const proj = d3.geoMercator().fitExtent([[15, 15], [width - 15, height - 15]], bboxGeoJSON);\n\n    const [x1, y1] = proj(nw);\n    const [x2, y2] = proj(se);\n\n    const centerPx = proj([-95.435, 29.740]);\n    const edgePx = proj([-95.435, 29.740 + 1 / 69.055]);\n    const milePx = Math.abs(edgePx[1] - centerPx[1]);\n\n    return { \n      projection: proj, \n      imgX: x1, \n      imgY: y1, \n      imgW: x2 - x1, \n      imgH: y2 - y1, \n      oneMilePx: milePx \n    };\n  }, []);\n\n  const { top5, others, markerPx } = React.useMemo(() => {\n    const mPx = projection(markerPos);\n    if (!data.length) return { top5: [], others: [], markerPx: mPx };\n\n    const withDist = data.map(d => ({\n      ...d,\n      dist: haversine(markerPos[0], markerPos[1], d.longitude, d.latitude),\n      px: projection([d.longitude, d.latitude])\n    })).sort((a, b) => a.dist - b.dist);\n\n    const t5 = withDist.slice(0, 5);\n    const oths = withDist.slice(5);\n    \n    const placedLabels = [];\n    \n    for (const shop of t5) {\n      const cx = shop.px[0];\n      const cy = shop.px[1];\n      const nameLen = shop.name.length;\n      const distStr = `${shop.dist.toFixed(2)}mi`;\n      const distLen = distStr.length;\n      \n      const w = Math.max(nameLen * 6, distLen * 4.8);\n      const h = 24;\n      \n      const candidates = [\n        { box: [cx + 9, cy - h/2, w, h], anchor: \"start\", textX: cx + 9, textY: cy - h/2 + 10 },\n        { box: [cx - 9 - w, cy - h/2, w, h], anchor: \"end\", textX: cx - 9, textY: cy - h/2 + 10 },\n        { box: [cx - w/2, cy - 9 - h, w, h], anchor: \"middle\", textX: cx, textY: cy - 9 - h + 10 },\n        { box: [cx - w/2, cy + 9, w, h], anchor: \"middle\", textX: cx, textY: cy + 9 + 10 },\n        { box: [cx + 9 + h, cy - h/2, w, h], anchor: \"start\", textX: cx + 9 + h, textY: cy - h/2 + 10 },\n        { box: [cx - 9 - w - h, cy - h/2, w, h], anchor: \"end\", textX: cx - 9 - h, textY: cy - h/2 + 10 },\n        { box: [cx - w/2, cy - 9 - h - h, w, h], anchor: \"middle\", textX: cx, textY: cy - 9 - h - h + 10 },\n        { box: [cx - w/2, cy + 9 + h, w, h], anchor: \"middle\", textX: cx, textY: cy + 9 + h + 10 }\n      ];\n      \n      for (const cand of candidates) {\n        const [bx, by, bw, bh] = cand.box;\n        \n        if (bx < 0 || bx + bw > 352 || by < 0 || by + bh > 400) continue;\n        \n        let intersect = false;\n        for (const p of placedLabels) {\n          const [px, py, pw, ph] = p;\n          if (!(bx >= px + pw || bx + bw <= px || by >= py + ph || by + bh <= py)) {\n            intersect = true;\n            break;\n          }\n        }\n        \n        if (!intersect) {\n          shop.label = { ...cand, distStr };\n          placedLabels.push(cand.box);\n          break;\n        }\n      }\n    }\n\n    return {\n      top5: t5,\n      others: oths,\n      markerPx: mPx\n    };\n  }, [data, markerPos, projection]);\n\n  const handlePointerDown = (e) => {\n    e.target.setPointerCapture(e.pointerId);\n    setIsDragging(true);\n  };\n\n  const handlePointerMove = (e) => {\n    if (!isDragging || !svgRef.current) return;\n    const rect = svgRef.current.getBoundingClientRect();\n    const x = e.clientX - rect.left;\n    const y = e.clientY - rect.top;\n    const [lon, lat] = projection.invert([x, y]);\n    setMarkerPos([lon, lat]);\n  };\n\n  const handlePointerUp = (e) => {\n    e.target.releasePointerCapture(e.pointerId);\n    setIsDragging(false);\n  };\n\n  return (\n    <div style={{\n      width: 352,\n      height: 400,\n      background: '#f7f0e6',\n      position: 'relative',\n      overflow: 'hidden',\n      fontFamily: '\"Space Grotesk\", sans-serif',\n      margin: 0,\n      padding: 0,\n      userSelect: 'none'\n    }}>\n      <svg\n        ref={svgRef}\n        width={352}\n        height={400}\n        style={{ position: 'absolute', top: 0, left: 0, touchAction: 'none' }}\n      >\n        {basemapImage && (\n          <image \n            href={basemapImage} \n            x={imgX} \n            y={imgY} \n            width={imgW} \n            height={imgH} \n            preserveAspectRatio=\"none\" \n            pointerEvents=\"none\"\n          />\n        )}\n\n        <circle cx={markerPx[0]} cy={markerPx[1]} r={oneMilePx} stroke=\"#94a3b8\" fill=\"none\" strokeWidth={1.5} strokeDasharray=\"4 4\" opacity={0.6} pointerEvents=\"none\" />\n        <circle cx={markerPx[0]} cy={markerPx[1]} r={oneMilePx * 2} stroke=\"#94a3b8\" fill=\"none\" strokeWidth={1.5} strokeDasharray=\"4 4\" opacity={0.6} pointerEvents=\"none\" />\n\n        {top5.map(shop => (\n          <line\n            key={`line-${shop.shop_id}`}\n            x1={markerPx[0]} y1={markerPx[1]}\n            x2={shop.px[0]} y2={shop.px[1]}\n            stroke=\"#334155\"\n            strokeWidth={1.5}\n            opacity={0.4}\n            pointerEvents=\"none\"\n          />\n        ))}\n\n        {others.map(shop => (\n          <circle\n            key={`other-${shop.shop_id}`}\n            cx={shop.px[0]} cy={shop.px[1]}\n            r={3.5}\n            stroke=\"#64748b\"\n            fill=\"transparent\"\n            strokeWidth={1.5}\n            onPointerEnter={() => setHoveredShop(shop)}\n            onPointerLeave={() => setHoveredShop(null)}\n            style={{ cursor: 'pointer' }}\n          />\n        ))}\n\n        {top5.map(shop => (\n          <g key={`top-${shop.shop_id}`}>\n            <circle\n              cx={shop.px[0]} cy={shop.px[1]}\n              r={5}\n              fill=\"#f97316\"\n              stroke=\"#f7f0e6\"\n              strokeWidth={1.5}\n              onPointerEnter={() => setHoveredShop(shop)}\n              onPointerLeave={() => setHoveredShop(null)}\n              style={{ cursor: 'pointer' }}\n            />\n            {shop.label && (\n              <text\n                textAnchor={shop.label.anchor}\n                fontFamily='\"JetBrains Mono\", monospace'\n                style={{ pointerEvents: 'none', paintOrder: 'stroke' }}\n                stroke=\"#f7f0e6\"\n                strokeWidth={3}\n                strokeLinejoin=\"round\"\n              >\n                <tspan x={shop.label.textX} y={shop.label.textY} fontSize={10} fill=\"#1e293b\" fontWeight=\"bold\">\n                  {shop.name}\n                </tspan>\n                <tspan x={shop.label.textX} y={shop.label.textY + 12} fontSize={8} fill=\"#475569\">\n                  {shop.label.distStr}\n                </tspan>\n              </text>\n            )}\n          </g>\n        ))}\n\n        <g\n          transform={`translate(${markerPx[0]}, ${markerPx[1]})`}\n          onPointerDown={handlePointerDown}\n          onPointerMove={handlePointerMove}\n          onPointerUp={handlePointerUp}\n          style={{ cursor: isDragging ? 'grabbing' : 'grab' }}\n        >\n          <circle r={16} fill=\"transparent\" />\n          <circle r={6} fill=\"#f7f0e6\" stroke=\"#1e293b\" strokeWidth={2} />\n          <circle r={2} fill=\"#1e293b\" />\n        </g>\n      </svg>\n\n      {hoveredShop && (\n        <div style={{\n          position: 'absolute',\n          left: Math.min(hoveredShop.px[0] + 12, 352 - 150),\n          top: Math.min(hoveredShop.px[1] + 12, 400 - 70),\n          background: '#f7f0e6',\n          border: '2px solid #1e293b',\n          boxShadow: '4px 4px 0px #1e293b',\n          padding: '8px 12px',\n          pointerEvents: 'none',\n          zIndex: 10,\n          width: 'max-content',\n          maxWidth: 140\n        }}>\n          <div style={{ fontSize: 12, fontWeight: 700, color: '#1e293b', marginBottom: 2, lineHeight: 1.2 }}>\n            {hoveredShop.name}\n          </div>\n          <div style={{ fontSize: 10, fontFamily: '\"JetBrains Mono\", monospace', color: '#475569', marginBottom: 4 }}>\n            {hoveredShop.neighborhood}\n          </div>\n          <div style={{ fontSize: 11, fontFamily: '\"JetBrains Mono\", monospace', color: '#f97316', fontWeight: 600 }}>\n            {hoveredShop.dist.toFixed(2)} mi\n          </div>\n        </div>\n      )}\n    </div>\n  );\n}",
  "outputs": {},
  "inputs_signature": {
    "basemap_image": "<input>",
    "data": "<input>"
  },
  "theme": {
    "name": "vibe-widgets",
    "description": null
  },
  "components": [],
  "model": "google/gemini-3.1-pro-preview",
  "audit": null,
  "save_inputs": {
    "embedded": false,
    "values": {}
  },
  "prompt_history": [
    {
      "prompt": "Houston donut shops.\n\nDATA: the `data` input is a list of shops, one row per shop, with columns: shop_id, name, neighborhood, latitude, longitude, rating (1-5), review_count, price_level (1-3), weekday_wait_min, weekend_wait_min, pumpkin_spice (boolean), open_late (boolean), signature_donut.\nBASEMAP: the `basemap` input is a GeoJSON FeatureCollection giving Houston context, bbox [-95.615, 29.600, -95.255, 29.880]. Each feature has properties.kind: \"bayou\" (LineString), \"loop\" (LineString ring for the Interstate 610 loop), or \"neighborhood\" (Point, with properties.name).\n\nMAP RULES: render the map as one inline SVG with d3 (import * as d3 from \"https://esm.sh/d3@7\"). Use d3.geoMercator with fitExtent on the basemap bbox so the whole bbox is visible inside the map area. Draw in this order: bayous as thin muted lines, the loop as a thin dashed ring, neighborhood names as small uppercase monospace labels in a low-emphasis color, then the shop marks on top. No tile layers, no external map libraries, no network requests at runtime other than the d3 import. Distances are great-circle miles from the haversine formula. Position the tooltip inside the map bounds.\n\nLAYOUT RULES: the widget is exactly 352x400 pixels, background #f2f0e9, no scrollbars, no responsiveness to window size. Fonts: 'Space Grotesk', sans-serif for text and 'JetBrains Mono', monospace for numbers, labels and small caps; both are provided by the host, do not load fonts.\n\nCOPY RULES (strict): no title or heading, no explanatory paragraphs, no instructions longer than one short line, no code or SQL readouts, no placeholder text, no emoji, no decorative badges, no record counts unless they are the result of the interaction. Every number shown is computed from the data.\n\nHypothesis: where is close?\nPlace a \"you are here\" marker at longitude -95.3848, latitude 29.7468, drawn as a white circle with a #1a1a1a outline and a dark center dot. Draw 1 and 2 mile rings as thin dashed circles around it. Draw every shop as a circle: the five nearest shops by haversine distance are filled #6b3fa0 with a white outline, connected to the marker by a thin #6b3fa0 line and labeled with shop name and distance in miles (two decimals); every other shop is a small muted hollow circle. The marker is draggable anywhere on the map (pointer events, live recompute while dragging). Hover on a shop shows a card tooltip with name, neighborhood, distance in miles.\n\nREVISION: Bug fix only. The map collapses every mark to a single point because the projection is fitted with fitExtent on a Polygon built from the bbox; d3-geo reads that ring's winding as the whole sphere. Replace that polygon with a MultiPoint of the two bbox corners: fitExtent(extent, { type: \"MultiPoint\", coordinates: [[-95.615, 29.600], [-95.255, 29.880]] }). Keep every other line, style, label and interaction exactly as it is.\n\nREVISION: Label placement fix only. The five nearest-shop labels (name and distance) pile on top of each other because the nearest shops cluster together. Place each label with a greedy search over candidate positions (right, left, above, below of its mark, then the same positions one label-height further out, with a 4px gap) and skip any label whose box would intersect an already placed label or leave the SVG. Keep the label text to the shop name on one line and the distance on a second smaller line, and give the text a 3px background-colored halo (paint-order: stroke). Change nothing else.\n\nREVISION: Two changes to make everywhere in this file, on top of whatever else this request asks for:\n\n1) REAL BASEMAP. A new input `basemap_image` is provided: a data: URL (JPEG) of a real, already-styled Houston street map -- roads, water, and neighborhood names are baked into the image itself. Its corners are NW corner [lon -95.615, lat 29.880], SE corner [lon -95.255, lat 29.600]. Read it with model.get(\"basemap_image\"). Render it FIRST, as a single SVG <image href={basemapImage}>: project the two bbox corners above with the SAME d3 projection used for the shop marks to get its on-screen rectangle (x, y, width, height from the two projected points), and set preserveAspectRatio=\"none\" so it exactly fills that rectangle -- this is what keeps the image aligned with the marks. Then DELETE every bit of code that draws your own roads, water, bayous, the 610 loop, neighborhood-name text labels, a background grid, or any bounding/frame circle around the whole map -- the image already shows all of that, and drawing your own on top would duplicate and clash with it. If the `basemap` GeoJSON input still exists in the code, remove every use of it along with any now-dead helper code, styles, or legend entries that only existed to describe those removed line-drawn elements. Keep every shop mark, overlay (target, search region, brush, connecting lines), tooltip and panel exactly as they are, just drawn on top of the new image instead of the old lines.\n\n2) POINTER MATH SAFETY. This widget renders inside an iframe inside a scaled presentation slide, which breaks two common shortcuts. Find every place in this file that converts a mouse/pointer position into a map coordinate and fix it if needed: NEVER use d3.drag's `event.x`/`event.y`, and NEVER use `d3.pointer(event, node)` -- both rely on SVGElement.getScreenCTM(), which does not account for an ancestor iframe living inside a CSS-transformed (scaled) page and silently returns the wrong offset there, producing a drag that visibly lags/jumps away from the cursor. Instead, always compute the position by hand: read the svg element's own `getBoundingClientRect()` and subtract its `left`/`top` from the event's `clientX`/`clientY` (inside a d3.drag `.on(\"drag\", ...)` handler, read those off `event.sourceEvent.clientX`/`event.sourceEvent.clientY`, not off `event.x`/`event.y`). If there is a rectangular brush/marquee-select interaction implemented with d3.brush(), replace it with a hand-rolled version using plain pointerdown/pointermove/pointerup listeners and an SVG <rect> you draw yourself, computing every corner the same getBoundingClientRect way -- d3.brush() has the identical getScreenCTM problem internally. Keep the resulting interaction pixel-identical to how it behaves today; this is a coordinate-math fix only, not a redesign.",
      "source": "create"
    }
  ]
}